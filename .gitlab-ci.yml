stages:
  - build

build_job:
  stage: build
  tags:
    - saas-windows-medium-amd64
  script:
    - "choco install python --version=3.12.0 -y --allow-downgrade --no-progress"
    - "$env:Path = [System.Environment]::GetEnvironmentVariable('Path','Machine') + ';' + [System.Environment]::GetEnvironmentVariable('Path','User')"
    - "python -m pip install --upgrade pip"
    - "python -m pip install nuitka customtkinter Pillow python-docx"

    - "echo 'Building Production Executable...'"
    - >
      python -m nuitka 
      --standalone 
      --onefile 
      --windows-console-mode=disable 
      --enable-plugin=tk-inter 
      --include-package=customtkinter
      --assume-yes-for-downloads 
      --windows-icon-from-ico=app_icon.ico 
      --include-data-file=app_icon.ico=app_icon.ico
      --onefile-tempdir-spec='%TEMP%/Click_Tool_%PID%'
      'Click!.py'

  artifacts:
    paths:
      - "Click!.exe"
    expire_in: never